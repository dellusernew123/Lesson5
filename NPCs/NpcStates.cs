public enum NpcStates
{
    Idle,
    Active
}

public enum ActiveStateVariations
{
    RunningAway,
    Chasing,
    Dying
}

public enum IdleStateVariations
{
    DoingNothing,
    Patrolling,
    RandomWalking
}
